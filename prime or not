#include <stdio.h>
#include <stdlib.h>


int main()
{
    int num, temp, rev=0;

    printf("Enter a number: ");
    scanf("%d", &num);

    while(num != 0)
    {
        temp = num%10;
        rev = rev * 10 + temp;
        num/=10;
    }

    printf("%d", rev);

    return 0;
}
